package com.wegongdu.rillway.core.model;

/**
 * Standard decision types across Human, Rule, and Agent actors.
 */
public enum DecisionType {
    APPROVE,
    REJECT,
    ROUTE,
    REQUEST_INFORMATION,
    ESCALATE
}
